{
  "name": "clinica-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pip install -r requirements.txt && python3 app.py",
    "build": "echo 'No node build step needed for Python'",
    "start": "python3 app.py",
    "lint": "echo 'No linting configured'"
  }
}
